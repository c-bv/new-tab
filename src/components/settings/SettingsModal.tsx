import styles from './SettingsModal.module.scss';

const SettingsModal = () => {
    return (
        <div id={styles.settingsModal}>
            SettingsModal
        </div>
    );
};

export default SettingsModal